import { NAME } from "./constants";
import { NAME as PARENT_NAME } from "../constants";

export const INCREASE = `${PARENT_NAME}/${NAME}/INCREASE`;
export const DECREASE = `${PARENT_NAME}/${NAME}/DECREASE`;
