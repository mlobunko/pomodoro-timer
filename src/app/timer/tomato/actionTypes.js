import { NAME as NAME_PARENT } from "../constants";
import { NAME } from "./constants";

export const SET_TIME = `${NAME_PARENT}/${NAME}/SET_TIME`;
export const DECREASE_TIME_BY_100MS = `${NAME_PARENT}/${NAME}/DECREASE_TIME_BY_100MS`;
