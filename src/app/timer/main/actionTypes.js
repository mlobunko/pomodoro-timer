import { NAME as PARENT_NAME } from "../constants";
import { NAME } from "./constants";

export const SET_TIMER_ON_TRUE = `${PARENT_NAME}/${NAME}/SET_TIMER_ON_TRUE`;
export const SET_TIMER_ON_FALSE = `${PARENT_NAME}/${NAME}/SET_TIMER_ON_FALSE`;
export const SET_WORKING_TIME_TRUE = `${PARENT_NAME}/${NAME}/SET_WORKING_TIME_TRUE`;
export const SET_WORKING_TIME_FALSE = `${PARENT_NAME}/${NAME}/SET_WORKING_TIME_FALSE`;

export const START_TIMER = `${PARENT_NAME}/${NAME}/START_TIMER`;
export const RESET_TIMER = `${PARENT_NAME}/${NAME}/RESET_TIMER`;
